id: TEST-0010
kind: test
title: ProjectItem relink is a project-level reference operation
given: One ProjectItem referenced by multiple TrackItems across one or more sequences.
when: A relink operation is requested.
then: The AI agent must treat relink as changing the project-level asset reference, not a single timeline instance.
expected: The plan reports affected timeline instances and requires review before relinking shared ProjectItems.
applies_to:
  - OBJ-0004
  - RULE-0002
  - RULE-0009
automation_level: semi_automated
