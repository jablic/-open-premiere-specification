id: TEST-0006
kind: test
title: Bin display name is not a stable unique identifier
given: Two bins with identical display names in different parts of the project hierarchy.
when: Automation targets a bin for moving, renaming, deletion, or import placement.
then: It must not select the bin by display name alone.
expected: Selection uses hierarchy context, explicit user confirmation, persistent identity when available, or a reviewable disambiguation step.
applies_to:
  - OBJ-0003
  - RULE-0002
automation_level: semi_automated
