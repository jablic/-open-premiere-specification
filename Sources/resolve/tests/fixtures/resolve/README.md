# Resolve fixtures

Four deterministic contract fixtures are present: 24 fps starts at 01h and 03h,
23.976 non-drop, and mixed tracks/markers. They are synthetic snapshots, not proof
of Resolve runtime behavior.

Resolve API field names, enumeration completeness, exact FPS representation and marker/item semantics are **Not documented — requires live fixture verification**.
